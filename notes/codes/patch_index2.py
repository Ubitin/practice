# 索引补丁（每处锚点都断言唯一）：① 修正收录数 ② 补回线 2 漏登记的行 ③ 检索表补行 ④ 日志补记
import io, sys

P = r'D:\lenovo\chat_with_deepseek_harness\讲义\00-知识索引与复习路线图.md'
s = io.open(P, encoding='utf-8').read()
orig = s

edits = []

# ① 收录数 105 → 104（体检结果：登记行 106 条、真实文件 104 个）
edits.append((
    '> 当前收录：**105 份**',
    '> 当前收录：**104 份**',
))

# ② 线 2 补回"先序后序层序"那一行（插在线 3 标题之前）
a2 = '\n\n### 线 3 · C++ 语言细节与输出工具'
row2 = ('\n| `先序后序层序-三种名单是什么` | **"什么时候记"决定名单的名字**：进格就记＝先序（父亲在前）／四个方向看完才记＝后序（儿子在前）／'
        'BFS 队列出队时记＝层序（按离起点远近排）——**代码只差 `push_back` 写在哪一行**。同一张 4×5 迷宫实测三种名单'
        '（先序 `(0,0)(1,0)(2,0)(3,0)(3,1)…`、后序 `(0,1)(0,2)(0,3)(0,4)(1,4)…`、层序 `(0,0)(1,0)(0,1)(2,0)…`，各 16 个）；'
        '⭐ **20000 组随机迷宫实验**：**后序删前缀 0 失败** ✅（CF377A 用的就是这个）、**层序删后缀 0 失败** ✅、'
        '**先序删前缀 3838 失败** ✗、**层序删前缀 2900 失败** ✗（含最小反例 7×2、k=6）。**归因**：先序前缀＝"从起点一路深入的必经之路"⇒挖空就断；'
        '后序前缀＝最深的末端⇒剪掉整棵小树枝。**选型**：要"区间"用先序（CF1006E 子树＝连续区间）、要"从叶子往回处理"用后序'
        '（删点保连通／树形 DP／表达式求值／判环）、要"距离最短"用层序（BFS）|')
edits.append((a2, row2 + a2))

# ③ 检索表：接在"栈参数"那一行后面
a3 = '更稳的办法是用迭代 DFS，两边都不用管） |'
rows3 = ('\n| `temp = \'#\' ? ... : ...` 为什么恒为真 | CF377A代码评审-墙标记写错与递归爆栈 §一 | '
         '`?:` 优先级高于 `=` ⇒ 实际是 `temp = ( \'#\' ? A : B )`，**条件就是字符常量 `\'#\'`（值 35）⇒ 恒真**，'
         '与网格内容完全无关 ⇒ 全图被标成墙。实测：真实 `\'#\'` 1 个 / `wall` 标记 12 个 / 后序名单 `timer = 1`（应 11）/ k=2 只写出 1 个 `X` |\n'
         '| 编译器能提前抓出 `=` 写成 `==` 吗 | CF377A代码评审-墙标记写错与递归爆栈 §一 | '
         '能。`-Wall`（含 `-Wparentheses`）报 `suggest parentheses around assignment used as truth value`；**改成 `==` 后警告消失**。'
         '交题前先跑 `g++ -Wall -Wextra -Wshadow` 零警告 |\n'
         '| 起点搜索的 `break` 只跳内层会怎样 | CF377A代码评审-墙标记写错与递归爆栈 §四.2 | '
         '`tx,ty` 变成"最后一个含 `.` 的行里最靠左的空格"，**不是全局第一个**。输入连通时无所谓（实测上 6 下 3、k=3 侥幸 OK）；'
         '多块数据会 FAIL（上 3 下 6、k=3 ⇒ 只剩 6/12 连通）|\n'
         '| 网格存成 `c[列][行]`（转置）行不行 | CF377A代码评审-墙标记写错与递归爆栈 §四.3 | '
         '**自洽就行**：存与取用同一映射就没错（实测非方阵 5×9、1×5、5×1 全对），但**判界必须配套写 `nx>m / ny>n`**；'
         '改成直觉的 `c[行][列]` 时界要同步换成 `nx>n / ny>m` |\n'
         '| 交题前必做的三件事 | CF377A代码评审-墙标记写错与递归爆栈 §二/§三 | '
         '① `-Wall -Wextra -Wshadow` 零警告；② 大迷宫本地别忘 `-Wl,--stack,268435456`；③ 答案不唯一的题写 checker，别比对固定输出 |')
edits.append((a3, a3 + rows3))

# ④ 更新日志：插在文件末尾分隔线之前
a4 = '\n---\n*维护约定：'
row4 = ('| 2026-09-25 | **补记（复核"我修好了" + 索引体检）**：① **确认改对了**——用户的 `01.cpp` 第 43 行已写成 `temp == \'#\' ? ...`，'
        '`-Wall -Wextra -Wshadow` 下 `-Wparentheses` 警告**完全消失**（只剩 `tx/ty may be used uninitialized`）；**全量实测**：'
        '3×4(k=2)／官方样例形状 3×3／非方阵 5×9(k=7)／`1×1`(k=0)／`1×5`／`5×1`／被墙包住的孤岛 **全部 checker OK** ✅；'
        '**500×500 全空地（25 万空格、k=20 万）1077ms OK**；**2000 组随机迷宫自动对拍：不合格 0 / 2000** ✅；'
        '**500×500 蛇形走廊（深度 12.5 万）默认栈 0xC00000FD、加 `-Wl,--stack,268435456` 后 41ms OK**。'
        '② **两组多块输入对照**（说明"起点随便挑"只在输入连通时成立）：上 6 格+下 3 格 / k=3 **侥幸 OK**（`break` 只跳内层 ⇒ 起点落在下面那块，大小恰好等于 k、被整块删掉）；'
        '上 3 格+下 6 格 / k=3 ⇒ **FAIL「只剩下 6/12 连通」**。③ ⚠️ **如实记录我自己的两次工具翻车**（`cf377a_stress.py`）：'
        '用默认编码读 checker 的**中文输出**，在 Windows 上 GBK 解码直接抛异常（于是"一发现失败脚本就崩"）；临时文件名写死 `_t_in.txt`/`_t_out.txt`，'
        '**两个对拍并发跑会互相覆盖** ⇒ 曾报出一组**假 FAIL**（seed 1014，单独重跑同一份代码判 **OK**）⇒ 教训"**测试工具本身也要被怀疑**"。'
        '④ **索引体检（顺手修）**：登记行 **106** 条 vs 真实讲义文件 **104** 个 ⇒ **收录数 105 → 104**；**补回缺失的线 2 行**'
        '`先序后序层序-三种名单是什么`（此前只在检索表与日志里有、线 2 表格漏登记）；查出**幽灵行** `string删除字符`（L73，实为指向 `STL讲义-01 §4` 的别名行、无独立文件）'
        '与两条 `char数组` 行同时出现在"五、自测与练习索引"表（属正常，非重复登记）。+`test_\\check_index.py`（索引体检脚本：登记行 vs 真实文件的对账）、'
        '+`test_\\patch_index.py`（本补丁，锚点唯一性断言）；手册 **§4.8** 补五条 CF377A 实测 ⚠️ 后重跑 docx |\n' + a4)
edits.append((a4, row4))

for old, new in edits:
    c = s.count(old)
    if c != 1:
        print('锚点出现 %d 次（应为 1），中止：%r' % (c, old[:70]))
        sys.exit(1)
    s = s.replace(old, new, 1)

io.open(P, 'w', encoding='utf-8', newline='').write(s)
print('四处修改全部完成 ✔')
print('行数 %d -> %d' % (orig.count('\n') + 1, s.count('\n') + 1))
