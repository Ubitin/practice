# 一次性完成索引的四处插入（每处都断言"锚点唯一"，避免改错行）
import io, sys

P = r'D:\lenovo\chat_with_deepseek_harness\讲义\00-知识索引与复习路线图.md'
s = io.open(P, encoding='utf-8').read()

edits = []

# ① 头部收录数 103 → 104
edits.append((
    '> 当前收录：**103 份**',
    '> 当前收录：**104 份**',
))

# ② 线 5 新增一行（锚在上一轮那一行的【结尾】）
a2 = '手册 **§4.8** 补"dfs 参数传什么 + 两种等价写法 + 名单要按 n*m 开"⚠️ 后重跑 docx |'
row2 = ('\n| `CF377A代码评审-墙标记写错与递归爆栈` | **CF377A 代码评审：用户说"我修好了"，复核确认修对了**'
        '（把 `temp = \'#\' ? wall[j][i]=1 : wall[j][i]=0;` 改成 `temp == \'#\' ? ...`）：'
        '⭐ **为什么原来必错**——`?:` 优先级高于 `=`，实际是 `temp = ( \'#\' ? A : B )`，**条件是字符常量 `\'#\'`（值 35）⇒ 恒为真**，'
        '**与网格内容完全无关** ⇒ 每一格都被标成墙。**探针实测量化**：真实 `\'#\'` 只有 **1** 个、`wall[][]` 却标了 **12** 个（3×4 全图）；'
        'dfs 后序名单 `timer = 1`（应 11）；k=2 时**只写出 1 个 `X`**（另一个坐标取自 `cnt[2]={0,0}` 的全局零值、写到 `c[0][0]`，打印循环到不了）'
        '⇒ checker **FAIL「变成墙的格子数 = 1，题目要求恰好 2」**。⚠️ 并拆穿"看着像对的"：那唯一的 X **恰好落在起点上**，输出只差一点 ⇒ '
        '**必须用 checker**，肉眼根本判不出来。⭐ **编译器早就在报警**：`-Wall`（含 `-Wparentheses`）在原版第 43 行连报两条 '
        '`suggest parentheses around assignment used as truth value`，**改成 `==` 后警告完全消失**（只剩 `tx/ty may be used uninitialized`）'
        '⇒ 铁律"**`=`/`==` 写错编译器一定会说，但只在 `-Wall` 下**"。**修正版全量验证**：3×4 / 官方样例形状 3×3 / **非方阵 5×9** / '
        '`1×1`(k=0) / `1×5` / `5×1` / 被墙包住的孤岛 —— **全部 checker OK** ✅；**500×500 全空地（25 万空格、k=20 万）1077ms OK**；'
        '**2000 组随机迷宫对拍不合格 0/2000** ✅。⚠️ **唯一真正的风险 = 递归爆栈**：500×500 蛇形走廊（深度 12.5 万）**默认栈退出码 '
        '`-1073741571 = 0xC00000FD`、输出 0 字节**，加 `-Wl,--stack,268435456` 后 **41ms 通过**（Linux 评测机栈大、本地最容易误判成"代码写错"）。'
        '另指出三件"顺手改"：`tx/ty` 未初始化（GCC 直接警告）；**起点搜索的 `break` 只跳出内层循环** ⇒ `tx,ty` 实际是"最后一个含 `.` 的行里最靠左的空格"'
        '（输入连通时无所谓：实测上 6 格+下 3 格/k=3 **侥幸 OK**；上 3 格+下 6 格/k=3 ⇒ **FAIL 只剩 6/12 连通**）；'
        '**行列转置存 `c[列][行]` 自洽但埋雷**（存/取同一映射就没错、实测非方阵全对，但判界必须配套写 `nx>m / ny>m`，改成直觉写法时极易漏改）。'
        '附 6 题自查 → 104 份；+`test_\\cf377a_user_v3.cpp`（**v3 出错版 verbatim 快照**）、+`test_\\cf377a_user_v3_probe.cpp`（探针：打印 wall 真值个数/名单长度/写 X 坐标）、'
        '+`test_\\cf377a_user_v4.cpp`（**已修版快照**）、+`test_\\cf377a_user_v3_fixed.cpp`（只改一行的对照版）、'
        '+`test_\\two_big.txt`（多块反例）、+`test_\\ns59.txt` / `e_1x1.txt` / `e_1x5.txt` / `e_5x1.txt` / `isl2.txt` / `all500.txt`；'
        '并**修好我自己的对拍工具两处 bug**（`cf377a_stress.py`：checker 中文输出未指定 `encoding=\'utf-8\'` 会 GBK 解码崩；'
        '临时文件名 `_t_in.txt` 不唯一，两个对拍并发会互相覆盖 ⇒ 假 FAIL）；手册 **§4.8** 补五条实测 ⚠️ 后重跑 docx |')
edits.append((a2, a2 + row2))

# ③ 检索表：接在上一轮"栈参数"那一行后面
a3 = '更稳的办法是用迭代 DFS，两边都不用管） |'
rows3 = ('\n| `temp = \'#\' ? ... : ...` 为什么恒为真 | CF377A代码评审-墙标记写错与递归爆栈 §一 | '
         '`?:` 优先级高于 `=` ⇒ 实际是 `temp = ( \'#\' ? A : B )`，**条件就是字符常量 `\'#\'`（=35）⇒ 恒真**，'
         '与网格内容无关 ⇒ 全图标成墙。实测：真实 `\'#\'` 1 个 / `wall` 标记 12 个 / `timer=1` / 只放 1 个 X |\n'
         '| 编译器能提前抓出 `=` 写成 `==` 吗 | CF377A代码评审-墙标记写错与递归爆栈 §一 | '
         '能。`-Wall`（含 `-Wparentheses`）报 `suggest parentheses around assignment used as truth value`；**改对后警告消失**。'
         '所以交前先跑 `g++ -Wall -Wextra -Wshadow` 零警告 |\n'
         '| 起点搜索的 `break` 只跳内层会怎样 | CF377A代码评审-墙标记写错与递归爆栈 §四.2 | '
         '`tx,ty` 变成"最后一个含 `.` 的行里最靠左的空格"，**不是全局第一个**。输入连通时无所谓；多块数据会 FAIL（实测上3下6/k=3 ⇒ 只剩 6/12 连通）|\n'
         '| 网格存成 `c[列][行]`（转置）行不行 | CF377A代码评审-墙标记写错与递归爆栈 §四.3 | '
         '**自洽就行**：存与取用同一映射就没错（实测非方阵 5×9、1×5、5×1 全对），但**判界必须配套写 `nx>m / ny>n`**；'
         '一旦改成直觉的 `c[行][列]`，界要同步换成 `nx>n / ny>m` |\n'
         '| 交题前 10 秒该做的三件事 | CF377A代码评审-墙标记写错与递归爆栈 §二/§三 | '
         '① `g++ -Wall -Wextra -Wshadow` 零警告；② 大迷宫（蛇形 500×500）本地别忘 `-Wl,--stack,268435456`；③ 答案不唯一的题写 checker，别比对固定输出 |')
edits.append((a3, a3 + rows3))

# ④ 更新日志：接在上一轮那一行的结尾
a4 = '+`test_\\param_maze.txt`（3×4 用例）；手册 **§4.8** 补"dfs 参数传什么 + 两种等价写法 + 后序名单按 n*m 开"⚠️ 后重跑 docx |'
row4 = ('\n| 2026-09-24 | +`CF377A代码评审-墙标记写错与递归爆栈`（**线 5**，回答"帮我看看我的代码问题出在哪里"→ 用户改完后"我修好了"的**复核**）：'
        '⭐ **定位到的致命 bug 只有一处**：第 43 行 `temp = \'#\' ? wall[j][i] = 1 : wall[j][i] = 0;` —— `?:` 优先级高于 `=`，'
        '实际结合成 **`temp = ( \'#\' ? (wall=1) : (wall=0) )`**，**条件写成字符常量 `\'#\'`（值 35）⇒ 恒为真** ⇒ 每一格都标成墙，'
        '**跟网格里到底有没有 `#` 毫无关系**。**探针实测（v3 出错版）**：网格真实 `\'#\'` = **1** 个，而 `wall[][]` 被标成 1 的 = **12** 个（3×4 全图）；'
        '起点搜到 `(x=1,y=3)`、dfs 后序名单 `timer = 1`（应 11）；k=2 时**写 `\'X\'` 的坐标是 `(1,3)` 和 `(0,0)`** —— 第二个来自 `cnt[2]={0,0}` 的**全局零初始化**，'
        '写到 `c[0][0]` 而打印循环只跑 1..n/1..m ⇒ **那一次写入"消失"**；checker 判 **FAIL「变成墙的格子数 = 1，题目要求恰好 2；剩下的空格数 10 != 11-2」**。'
        '⚠️ 并拆穿"看着像对的"：它唯一放下的那个 X **恰好就在起点那格**，输出 `.... / ..#. / X...` 只差一点点 ⇒ **这正是必须写 checker 的理由**。'
        '⭐ **编译器本来就能拦住它**：`-Wall`（`-Wparentheses` 属于 `-Wall`）在原版第 43 行连报两条 '
        '`warning: suggest parentheses around assignment used as truth value`（两个分支各一条）；**用户把 `=` 改成 `==` 后，这两条警告完全消失**'
        '（只剩 `\'tx\' may be used uninitialized`），这是"改对了"最硬的证据 ⇒ 铁律"**`=`/`==` 写错编译器一定会说，但只在 `-Wall` 下**"。'
        '**修正版全量验证**：3×4(k=2)、官方样例形状 3×3、**非方阵 5×9(k=7)**、`1×1`(k=0)、`1×5`、`5×1`、被墙整个包住的孤岛 —— **全部 checker OK** ✅；'
        '**500×500 全空地（25 万个空格、k=20 万）1077ms OK**（也证明 `dot cnt[maxn*maxn]` = 252004 刚好装得下 250000）；'
        '**2000 组随机迷宫自动对拍：不合格 0 / 2000** ✅。⚠️ **唯一真正的风险 = 递归爆栈**：500×500 **蛇形走廊**（深度 12.5 万）'
        '**默认栈退出码 -1073741571 = 0xC00000FD、输出 0 字节**（一行都没有），同一份代码加 `-Wl,--stack,268435456` 后 **41ms 通过、checker OK** '
        '⇒ 典型的"本地崩、评测机（Linux 栈大）可能过"，最容易让人白排查。**另指出三件"顺手改"**：① `ll tx,ty;` 未初始化（GCC `-Wmaybe-uninitialized` 直接警告，'
        '本题数据碰不到但一行就能消掉）；② **起点搜索的 `break` 只跳出内层循环** ⇒ `tx,ty` 实际是"**最后一个含 `.` 的行里、最靠左的空格**"而非全局第一个'
        '（输入连通时无所谓：实测"上 6 格+下 3 格 / k=3"**侥幸 OK**——底部块大小恰好等于 k、被整块删掉；而"上 3 格+下 6 格 / k=3" ⇒ **FAIL 只剩 6/12 连通**）；'
        '③ **行列转置存 `c[列][行]` 自洽但埋雷**（存与取用同一映射、判界配套 `nx>m / ny>n` 就没错，实测非方阵全对；但一旦改成直觉的 `c[行][列]` 而忘了换界就是越界/漏搜）。'
        '⚠️ **并如实记录我自己的两次工具翻车**：`cf377a_stress.py` ① 用默认编码读 checker 的**中文输出**在 Windows 上 GBK 解码直接抛异常，导致"一发现失败就崩"；'
        '② 临时文件名写死 `_t_in.txt`/`_t_out.txt`，**两个对拍并发跑会互相覆盖** ⇒ 曾报出一组**假 FAIL**（seed 1014：输入是 A 组、输出被 B 组覆盖，'
        '单独重跑同一份代码判 **OK**）—— 教训"**测试工具本身也要被怀疑**"。附 6 题自查 → **104 份**；'
        '+`test_\\cf377a_user_v3.cpp`（v3 出错版 verbatim 快照，**未改动原 `01.cpp`**）、+`test_\\cf377a_user_v3_probe.cpp`（探针版：打印 wall 真值个数 / 名单长度 / 写 X 的坐标）、'
        '+`test_\\cf377a_user_v4.cpp`（**用户已修版快照**）、+`test_\\cf377a_user_v3_fixed.cpp`（只改一行的对照版）、+`test_\\two_big.txt`（多块反例）、'
        '+`test_\\ns59.txt`、`e_1x1.txt`、`e_1x5.txt`、`e_5x1.txt`、`isl2.txt`、`all500.txt`；手册 **§4.8** 补五条实测 ⚠️ 后重跑 docx |')
edits.append((a4, a4 + row4))

for old, new in edits:
    c = s.count(old)
    if c != 1:
        print('锚点出现 %d 次（应为 1 次），中止：%s' % (c, old[:60]))
        sys.exit(1)
    s = s.replace(old, new)

io.open(P, 'w', encoding='utf-8', newline='').write(s)
print('四处插入全部完成 ✔')
print('文件行数 =', s.count('\n') + 1)
