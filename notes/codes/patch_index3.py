# 把"打磨版 + 独立工具复测"的证据补进讲义与索引日志（每处锚点断言唯一）
import io, sys

JY = r'D:\lenovo\chat_with_deepseek_harness\讲义\CF377A代码评审-墙标记写错与递归爆栈.md'
IDX = r'D:\lenovo\chat_with_deepseek_harness\讲义\00-知识索引与复习路线图.md'

def patch(path, edits):
    s = io.open(path, encoding='utf-8').read()
    for old, new in edits:
        c = s.count(old)
        if c != 1:
            print('锚点出现 %d 次（应为 1），中止 %s：%r' % (c, path, old[:60]))
            sys.exit(1)
        s = s.replace(old, new, 1)
    io.open(path, 'w', encoding='utf-8', newline='').write(s)
    print('已更新：', path)

# ---- 讲义：① §二 表格补两行；② §四.2 补一句"打磨版在多块数据上也会挂" ----
a1 = '| **2000 组随机迷宫自动对拍** | **不合格 0 / 2000 组** ✅（生成器保证空格连通、k ≤ 空格数−1） |'
n1 = (a1 + '\n'
      '| **换一套独立工具再跑**（`fast_stress.py`：生成器与 checker 都用 Python 重新实现，与上面那套不是同一份代码） | '
      '**不合格 0 / 3000（8×8 图）+ 0 / 300（60×60 图）** ✅ |\n'
      '| **打磨版** `test_\\cf377a_polished.cpp`（行优先存储 + 起点跳两层 + 给初值 + 合并 if） | '
      '`-Wall -Wextra -Wshadow` **零警告**；7 个连通用例 + 蛇形走廊 + 25 万空格全 OK；'
      '**不合格 0 / 3000（8×8）+ 0 / 300（60×60）** ✅ |')

a2 = '（题面保证有解、数据里初始空格连通，所以这两组只是"如果数据不规范会怎样"。'
n2 = ('（题面保证有解、数据里初始空格连通，所以这两组只是"如果数据不规范会怎样"。'
      '⚠️ **注意：把起点改成"真正的第一个空格"（打磨版那样）在多块数据上同样会挂**，只是挂法不同 —— '
      '实测 `two.txt`（上 6 下 3、k=3）从上面那块删 ⇒ 只剩 3/6 连通；`two_big.txt` 同理只剩 3/12。'
      '**"从哪个空格出发都行"这句话的前提就是输入连通**。')

patch(JY, [(a1, n1), (a2, n2)])

# ---- 索引：日志那一行补上"打磨版 + 独立复测" ----
a3 = '手册 **§4.8** 补五条 CF377A 实测 ⚠️ 后重跑 docx |'
n3 = ('手册 **§4.8** 补五条 CF377A 实测 ⚠️ 后重跑 docx；⑤ **另交付打磨版** `test_\\cf377a_polished.cpp`'
      '（行优先 `c[行][列]` + 起点搜索跳两层 + `tx,ty` 给初值 + dfs 判重合成一行）：`-Wall -Wextra -Wshadow` **零警告**，'
      '7 个连通用例 + 蛇形走廊 + 25 万空格全 OK；并**另写一套独立对拍工具** `test_\\fast_stress.py`'
      '（生成器与 checker 全部用 Python 重新实现、每轮只 spawn 一次待测程序，比旧脚本快一个量级）：'
      '**你的 v4 与打磨版各 0/3000（8×8）+ 0/300（60×60）** ✅；⚠️ 同时如实说明"**从哪个空格出发都行**"的前提是**输入连通**——'
      '打磨版在 `two.txt`/`two_big.txt` 上同样 FAIL（只剩 3/6、3/12），与你的原版只是挂法不同 |')

patch(IDX, [(a3, n3)])
print('全部完成 ✔')
